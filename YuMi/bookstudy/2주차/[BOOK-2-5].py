from max import max_of

#각 배열 원소의 최대값을 구해서 출력하기

t = (4,7,5.6,2,3.14,1)
s ='string'
a = ['DTS','AAC','FLAC']

print(f't의 최대값은 : {max_of(t)}입니다')
print(f's의 최대값은 : {max_of(s)}입니다') #가장 큰 문자코드
print(f'a의 최대값은 : {max_of(a)}입니다') #사전순으로 큰 문자열